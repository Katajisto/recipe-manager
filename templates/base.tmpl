{{define "prefix"}}
<!DOCTYPE html>
<html>
	<head>
		<title>Omnom</title>
		<link rel="stylesheet" href="/static/build.css" />
		<script src="/static/vendor/htmx.min.js"></script>
	</head>
	<body>
		<nav class="flex bg-green-700 text-white items-center justify-between px-5 py-2">
			<a href="/" class="text-xl">Omnom</a>
			<div class="flex">
				<button class="m-0 text-md rounded bg-green-900 p-1">Log out</button>
			</div>
		</nav>
		<!-- Include main -->
{{end}}

{{define "suffix"}}
	</body>
</html>
{{end}}