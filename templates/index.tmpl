{{ template "prefix" }}
<div id="app" class="flex p-5">
	<div id="leftpanel" class="w-1/3 p-2 text-green-600 rounded bg-green-100">
		{{ template "recipesList" .}}
	</div>
	<div id="centralpanel" class="w-2/3 ml-2 p-2 flex-col items-center text-green-600 rounded bg-green-100">
		{{ template "centralPanelDefault" . }}
	</div>
</div>
{{ template "suffix" }}
